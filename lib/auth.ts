// Hardcoded credentials — replace with a real auth system later
export const ADMIN_USERNAME = "admin";
export const ADMIN_PASSWORD = "password123";

export const SESSION_COOKIE = "admin_session";
// A simple secret token stored in the cookie — not a real JWT, just a fixed secret
export const SESSION_SECRET = "chatblog_secret_session_2024";
